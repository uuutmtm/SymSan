name: SymSan
channels:
  - conda-forge
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch==2.8.0
      - transformers>=5.3.0
      - accelerate>=1.12.0
      - numpy
      - pandas
      - tqdm
